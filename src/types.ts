export type TPosition = {
  x: number,
  y: number,
}

export type TWays = {
  svg: SVGImageElement;
  x: number;
  y: number;
  round: {
    minX: number,
    maxX: number,
    minY: number,
    maxY: number,
  },
}
